/**
 * UI fonts are part of the application shell. Terminal/code fonts are loaded by
 * the terminal chunks so users who never open a terminal do not download them.
 */
import '@fontsource-variable/inter/wght.css';
import '@fontsource-variable/geist/wght.css';
